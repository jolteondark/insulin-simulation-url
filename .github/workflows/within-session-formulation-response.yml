name: Within-session formulation response
on:
  push:
    branches: [v2/state-space-minimal]
    paths:
      - 'scripts/audit_within_session_formulation_response.py'
      - '.github/workflows/within-session-formulation-response.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v2/state-space-minimal
      - run: |
          git clone --quiet https://github.com/MouzKarrigan/2024_TJU_Data_Mining-Analysis.git /tmp/tju
          cd /tmp/tju
          git checkout --quiet cf5e7f32e8f8295e49df27ea70d9d6b21ab30598
      - run: python3 scripts/audit_within_session_formulation_response.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add analysis/within_session_formulation_response
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Store within-session formulation response audit'
          git push origin HEAD:v2/state-space-minimal
