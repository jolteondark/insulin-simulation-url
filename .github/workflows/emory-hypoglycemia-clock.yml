name: Emory hypoglycemia clock audit
on:
  workflow_dispatch:
  push:
    branches: [v2/state-space-minimal]
    paths:
      - 't2dm_patient_phenotype_v3_inpatient_mix_exp.js'
      - 't2dm_treatment_policy_weight_bg_exp.js'
      - 't2dm_inpatient_dynamic_v1_exp.js'
      - 't2dm_inpatient_course_v1_exp.js'
      - 't2dm_inpatient_trajectory_v1_exp.js'
      - 'insulin_prandial_pk_prior_ranges_exp.js'
      - 'scripts/audit_emory_hypoglycemia_clock.js'
      - '.github/workflows/emory-hypoglycemia-clock.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v2/state-space-minimal
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run audit and preserve report on structural failure
        run: |
          set +e
          node scripts/audit_emory_hypoglycemia_clock.js
          code=$?
          echo "AUDIT_EXIT=$code" >> "$GITHUB_ENV"
          exit 0
      - name: Commit report
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add analysis/emory_hypoglycemia_clock
          git diff --cached --quiet || git commit -m 'Update Emory hypoglycemia clock audit'
          git push origin HEAD:v2/state-space-minimal
      - name: Propagate audit status
        run: exit "${AUDIT_EXIT:-0}"
