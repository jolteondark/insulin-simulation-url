name: v2-validation
on:
  push:
    branches: [v2/state-space-minimal]
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run persistent memory coupling grid
        run: node validation_persistent_memory_coupling_node.js
      - uses: actions/upload-artifact@v4
        with:
          name: persistent-memory-coupling-result
          path: persistent_memory_coupling_result.json
